const GenerateTeams = ({ onClick }) => ( 
    <>
        <div>
            <button
                onClick= { onClick }
            >
                Generate Teams
            </button>
        </div>
    </>
);

export default GenerateTeams;