const GenerateTeams = ({ onClick }) => ( 
    <>
        <button
            className="doButton"
            onClick= { onClick }
        >
            Generate Teams
        </button>
    </>
);

export default GenerateTeams;